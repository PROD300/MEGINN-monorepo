# Monorepo Migration — чек-лист

> Исторический документ (2026-08-09/10). Проект и репозиторий назывались `OBSIDIAN` на момент
> этой миграции; 2026-08-17 переименованы в `MEGINN` (тот же проект, тот же код, тот же git —
> см. `PROJECT.md` и `HANDOFF_COWORK_2026-08-17.md`). Имена ниже намеренно оставлены как были,
> чтобы не исказить исторические URL/команды/пути.

Цель: объединить корень `OBSIDIAN/` (документы) и `app/` (код, отдельный git-репозиторий
с remote на `PROD300/OBSIDIAN.git`, задеплоен на Vercel) в единый монорепозиторий с
сохранённой покоммитной историей `app/` (вариант A — `git subtree`), не рискуя текущим
рабочим деплоем.

Полное обсуждение решения и альтернатив — в истории диалога с Claude Code, 2026-08-09/10.

**Правило:** ничего не выполняется без явной команды пользователя начать конкретную фазу.
Каждый шаг ниже отмечается сразу после фактического выполнения, не заранее.

---

## Фаза 0 — страховка (не трогает ничего рабочего)

- [x] Тег `backup-pre-monorepo-2026-08-10` создан на текущем HEAD `app/`
      (`576dd7f` — "fix: drop TopNav from Onboarding, center wizard vertically, narrow step cards")
- [x] Тег запушен в `origin` (`PROD300/OBSIDIAN.git`)
- [x] Зеркало-бэкап всего репозитория `app/` запушено в отдельный приватный репозиторий
      `PROD300/OBSIDIAN-backup.git` (`git push --mirror` — main + все теги, включая
      `backup-pre-monorepo-2026-08-10`)

## Фаза 1 — сборка монорепо в отдельном месте (вариант A, git subtree) — ЗАВЕРШЕНА

- [x] Создана отдельная рабочая папка: `OBSIDIAN-monorepo/` (сиблинг `OBSIDIAN/`, вне `app/`)
- [x] `git init`, первый коммит `2e6a970` — снапшот документов (`briefs/`, `prd/`, `ia/`, `ux/`,
      `ds/`, `research/`, `landing/`, `.claude/`, `_archive/`, `audit/`, `dashboard/`,
      `CLAUDE.md` и т.д.), без `app/`, без `.tmp/`, без мусорного файла
      `System.Collections.Hashtable`
- [x] История `app/` подтянута коммитом `8d8c987` через
      `git subtree add --prefix=app https://github.com/PROD300/OBSIDIAN.git main`
      — все 16 коммитов `app/` (от `9607384` до `576dd7f`) сохранены как родительская
      ветка merge-коммита, видны на `git log --graph`
- [x] Итоговое дерево проверено: `app/src/...` на месте, файлы совпадают с оригиналом,
      история подтверждена через `git log --oneline --graph`

**Расположение нового монорепо:** `C:\Users\dasha\OneDrive\Рабочий стол\OBSIDIAN-monorepo\`
**HEAD:** `8d8c987`. Пока без remote — только локально.

## Фаза 2 — проверка деплоя без риска для прода

- [x] Новый монорепо запушен в новый приватный репозиторий
      `PROD300/OBSIDIAN-monorepo.git` (`main` @ `8d8c987`)
- [x] Новый Vercel-проект создан с Root Directory = `app/` — https://obsidian-monorepo.vercel.app
- [x] Сборка на новом проекте прошла успешно
- [x] Технически сверено: `/` и `/portfolio` отдают 200 на обоих деплоях, HTML-шелл
      побайтово идентичен, JS-бандл `index-DrDJvMJ3.js` совпадает по MD5 —
      сборка из монорепо идентична текущему проду (`obsidian-peach-sigma.vercel.app`)
- [ ] Финальная визуальная сверка глазами пользователя — не подтверждена

## Фаза 3 — переключение — ЗАВЕРШЕНА

- [x] Решено: дальнейшая работа — в `OBSIDIAN-monorepo/`, старые `OBSIDIAN/` и `app/`
      замораживаются как архив, не редактируются дальше
- [x] Кастомного домена нет — `https://obsidian-monorepo.vercel.app/` зафиксирован как живая
      ссылка проекта в `PROJECT.md` (в монорепо)
- [x] `PROJECT.md` обновлён: GitHub URL → `PROD300/OBSIDIAN-monorepo`, живая ссылка → новый
      деплой, старые репозиторий/зеркало/деплой отмечены как архив (коммит `b79e0eb`, запушен)
- [x] Старый репозиторий (`PROD300/OBSIDIAN`), зеркало (`PROD300/OBSIDIAN-backup`) и старый
      Vercel-деплой (`obsidian-peach-sigma.vercel.app`) оставлены нетронутыми — работают
      как архив на переходный период, ничего не удалено

### Открытый follow-up (не блокирует миграцию)

- [ ] `dashboard/app/config.js` — сгенерирован директивой `/dashboard`, не редактировался
      вручную, всё ещё указывает `prototypeUrl` на старую архивную ссылку. Перегенерировать
      через `/dashboard`, когда понадобится продолжить юзер-тесты на новом деплое.

---

**Текущий статус:** Фазы 0, 1 и 2 завершены (кроме визуальной сверки глазами — ждёт
подтверждения пользователя). Монорепо `PROD300/OBSIDIAN-monorepo` (приватный) задеплоен
на Vercel и технически идентичен продакшену. Ничего в `OBSIDIAN/`, `app/` или их
GitHub/Vercel не изменено — оригиналы нетронуты, старый прод работает как прежде.
Фаза 3 (переключение) не начата, ждёт явной команды.
