# Monorepo Migration — чек-лист

Цель: объединить корень `OBSIDIAN/` (документы) и `app/` (код, отдельный git-репозиторий
с remote на `PROD300/OBSIDIAN.git`, задеплоен на Vercel) в единый монорепозиторий с
сохранённой покоммитной историей `app/` (вариант A — `git subtree`), не рискуя текущим
рабочим деплоем.

Полное обсуждение решения и альтернатив — в истории диалога с Claude Code, 2026-08-09/10.

**Правило:** ничего не выполняется без явной команды пользователя начать конкретную фазу.
Каждый шаг ниже отмечается сразу после фактического выполнения, не заранее.

---

## Фаза 0 — страховка (не трогает ничего рабочего)

- [x] Тег `backup-pre-monorepo-2026-08-10` создан на текущем HEAD `app/`
      (`576dd7f` — "fix: drop TopNav from Onboarding, center wizard vertically, narrow step cards")
- [x] Тег запушен в `origin` (`PROD300/OBSIDIAN.git`)
- [x] Зеркало-бэкап всего репозитория `app/` запушено в отдельный приватный репозиторий
      `PROD300/OBSIDIAN-backup.git` (`git push --mirror` — main + все теги, включая
      `backup-pre-monorepo-2026-08-10`)

## Фаза 1 — сборка монорепо в отдельном месте (вариант A, git subtree)

- [ ] Создана отдельная рабочая папка для нового монорепо (вне `OBSIDIAN/`, вне `app/`)
- [ ] `git init` в новой папке, первый коммит с текущим состоянием документов
      (`briefs/`, `prd/`, `ia/`, `ux/`, `ds/`, `research/`, `landing/`, `CLAUDE.md` и т.д.,
      без `app/`, без мусорных/временных файлов)
- [ ] История `app/` подтянута через `git subtree add --prefix=app <url-или-путь-до-app> main`
      с сохранением всех коммитов
- [ ] Итоговое дерево проверено (файлы на месте, история `app/` видна в `git log -- app/`)

## Фаза 2 — проверка деплоя без риска для прода

- [ ] Новый монорепо запушен в новый (не в текущий) GitHub-репозиторий
- [ ] Новый Vercel-проект создан с Root Directory = `app/`
- [ ] Сборка на новом проекте прошла успешно
- [ ] Визуально сверено: новый деплой идентичен текущему проду

## Фаза 3 — переключение (только по явной команде пользователя)

- [ ] Домен/продакшен-настройки перенесены на новый Vercel-проект
- [ ] Старый репозиторий/деплой сохранён как архив на переходный период

---

**Текущий статус:** Фаза 0 завершена полностью — тег-страховка и зеркало-бэкап готовы.
Фаза 1 не начата, ждёт явной команды.
